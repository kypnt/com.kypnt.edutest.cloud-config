
application:
  fileName: application-uat.yml
  env: ${spring.profiles.active}
  
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: localhost:8761/eureka/eureka
    instance:
      hostname: localhost
