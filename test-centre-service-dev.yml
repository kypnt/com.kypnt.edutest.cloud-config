
application:
  message: Configured by Azure Spring Cloud (TEST_CENTRE-dev)

server:
  port: 9001

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: TEST-CENTRE-SERVICE